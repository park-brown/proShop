import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
	root: {
		// fontSize: '1rem', it didn't wort after refresh page , has to override it
	},
}));
